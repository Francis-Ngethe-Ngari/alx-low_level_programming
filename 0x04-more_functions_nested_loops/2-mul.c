/* Declare Header Files */
#include "main.h"

/**
 * mul - function declared to multiple two integers
 *
 * main - Entry point
 *
 * @a: parameter passed to function
 * @b: parameter passed to function
 *
 * Return: outputs arguement passed to it
 *
 */

int mul(int a, int b)
{
	int mult;

	mult = (a * b);

	return (mult);
}
