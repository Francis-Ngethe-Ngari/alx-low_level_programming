/* Declare Header Files */
#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
/* Declaring variable ch */

char ch = 'z';

for (ch = 'z'; ch >= 'a'; ch--)

putchar(ch);
putchar('\n');

return (0);
}
